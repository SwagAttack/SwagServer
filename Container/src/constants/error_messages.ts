export enum ErrorMessages {
    /**
     * Registerable decorator already exists on target.
     */
    ExistingRegisterableDecorator = "@registerable decorator already exists on target.",
    /**
     * Inject decorator undefined
     */
    InjectDecoratorUndefined = "Service identifier undefined in @inject decorator.",

}